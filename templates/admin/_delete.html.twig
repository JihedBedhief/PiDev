<form method="post" action="{{ path('app_adminrh_delete', {'id': rh.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rh.id) }}">
    <button class="btn">Delete</button>
</form>
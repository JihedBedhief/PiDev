{% extends 'base1.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>matricule</th>
                <td>{{ user.matricule }}</td>
            </tr>
            
            <tr>
                <th>domaine</th>
                <td>{{ user.domaine }}</td>
            </tr>
            <tr>
                <th>Numero_tele</th>
                <td>{{ user.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
           
            <tr>
                <th>adresse</th>
                <td>{{ user.adresse }}</td>
            </tr>
           
            <tr>
                <th>abonnement</th>
                <td>{{ user.abonnement}}</td>
                
            </tr>
            <tr>
                <th>statut</th>
                <td>{{ user.statuts }}</td>
            </tr>
         
        </tbody>
    </table>
    <center>
                <a class="btn btn-success" href="{{ path('user_status_edit', {'id': user.id}) }}">edit statuts</a>
    </center>

{% endblock %}
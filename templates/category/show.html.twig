
{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}
	 <h1>Category</h1>
	<div style="margin-left:320px ; width:78% ; margin-top:100px">

		<table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>
	</div>
	<center>
		<a class="btn btn-success" href="{{ path('app_category_index') }}" style="margin-left:180px">back to list</a>

		
		<a class="btn btn-danger">{{ include('category/_delete_form.html.twig') }}</a></br>

	</center>
{% endblock %}

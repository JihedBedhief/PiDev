

{% extends 'base.html.twig' %}

{% block title %}Product
{% endblock %}

{% block body %}
	 <h1>Product</h1>
	<div style="margin-left:320px ; width:78% ; margin-top:100px">

		 <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name_product</th>
                <td>{{ product.nameProduct }}</td>
            </tr>
            <div style="display:none">
             <tr>
             <th>image</th>
                <td><img width="79" src="/upload/img/{{ product.image }}" ><td>
            </tr>
            </div>
            <tr>
                <th>prix</th>
                <td>{{ product.prix }}</td>
            </tr>
        
        </tbody>
    </table>
	</div>
	<center>
		<a class="btn btn-success" href="{{ path('app_product_index') }}" style="margin-left:180px">back to list</a>

		
		<a class="btn btn-danger">{{ include('product/_delete_form.html.twig') }}</a></br>

	</center>
{% endblock %}

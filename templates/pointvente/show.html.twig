{% extends 'base.html.twig' %}

{% block title %}PointVente{% endblock %}

{% block body %}
    <h1>PointVente</h1>
<div style="margin-left:320px ; width:78% ; margin-top:100px">
    <div style="width :120%"> 
    <table class="table">
        <tbody>
          
            <tr>
                <th>Nom</th>
                <td>{{ point_vente.name }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ point_vente.region }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ point_vente.ville }}</td>
            </tr>
            <tr>
                <th>Code Postal</th>
                <td>{{ point_vente.codePostal }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ point_vente.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ point_vente.email }}</td>
            </tr>
        </tbody>
    </table>
</div>
<center>
    <a href="{{ path('app_point_vente_index') }}">back to list</a>

    <a href="{{ path('app_point_vente_edit', {'id': point_vente.id}) }}">edit</a>

    {{ include('point_vente/_delete_form.html.twig') }}
</center>
{% endblock %}

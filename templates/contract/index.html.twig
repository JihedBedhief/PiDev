{#{% extends 'base.html.twig' %}

{% block title %}Contract index{% endblock %}

{% block body %}
    <h1>Contract index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Duration</th>
                <th>Salary</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contract in contracts %}
            <tr>
                <td>{{ contract.id }}</td>
                <td>{{ contract.type }}</td>
                <td>{{ contract.duration }}</td>
                <td>{{ contract.salary }}</td>
                <td>
                    <a href="{{ path('app_contract_show', {'id': contract.id}) }}">show</a>
                    <a href="{{ path('app_contract_edit', {'id': contract.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_contract_new') }}">Create new</a>
     <td>{{ blog.employer}}</td>
{% endblock %}#}
{% extends 'base.html.twig' %}


{% block body %}


<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">

    <div class="col-lg-6">

        <div class="card">
            <div class="card-body">
                <a href="{{ path('app_rh_index') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
               
                <h5 class="card-title"> Contracts</h5>
               

                <!-- Table with stripped rows -->
                <table class="table table-striped">
                    <thead>
                    <tr>     <th>Employer name</th>
                        <th>Type</th>
                        <th>Duration</th>
                        <th>Salary</th>
                
                    </tr>
                    </thead>
                    <tbody>
                    {% for blog in contracts %}
                        <tr>
                           
                        <td>{{ blog.employer}}</td>
                            <td>{{ blog.type }}</td>
                             <td>{{ blog.duration }}</td>
                            <td>{{ blog.salary }}</td>
                            <td>
                                <a class="btn btn-success" href="{{ path('app_contract_show',{'id':blog.id}) }}">View details</a>
                            </td>
                            <td>
                                <a class="btn btn-success" href="{{ path('app_contract_edit',{'id':blog.id}) }}">Edit contract</a>
                            </td>
                           
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
                <!-- End small tables -->

            </div>
        </div>

    </div>

 

</div>
{#{% block add %}
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 50px;">
<a class="btn btn-success" href="{{ path('app_contract_new') }}" style="margin-left:2%">Show</a>
</div>
{% endblock %}#}
{% endblock %}
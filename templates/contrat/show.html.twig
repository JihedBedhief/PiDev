{# {% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ contrat.type }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ contrat.salaire }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ contrat.datedebut ? contrat.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ contrat.datefin ? contrat.datefin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_index') }}">back to list</a>

    <a href="{{ path('app_contrat_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %} #}


{% extends 'base.html.twig' %}


{% block body %}


<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">

    <div class="col-lg-6">

        <div class="card">
            <div class="card-body">
                <a href="{{ path('app_employees_index') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
				<div class="col-sm-6">
      <div class="d-grid">
	<a class="btn btn-success create-pdf-btn" href="{{ path('app_contract_pdf', {'id': contrat.id}) }}">Download as pdf</a>
	<style>
		.create-pdf-btn{

		float: right;
  margin-top: -40px;
  margin-left: 150%;
  width: 10rem;
  background-color: rgb(red, green, 0);
  color: white;
		}

	</style>
      </div>
    </div>
                <h5 class="card-title"> Contract Detaill</h5>

                {% block title %}{% endblock %}


    
		<table class="table">
			<tbody>
			 <tr>
              <th>Nom</th>
              <td>{{ contrat.emp.nom }}</td>
            </tr>
            <tr>
              <th>Type</th>
              <td>{{ contrat.type }}</td>
            </tr>
            <tr>
              <th>Salaire</th>
              <td>{{ contrat.salaire }}</td>
            </tr>
            <tr>
              <th>Date debut</th>
              <td>{{ contrat.datedebut ? contrat.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
              <th>Date fin</th>
             <td>{{ contrat.datefin ? contrat.datefin|date('Y-m-d') : '' }}</td>
            </tr>
			</tbody>
		</table>

		<div class="row mt-3">
    <div class="col-sm-6 mb-3 mb-sm-0">
    <div class="d-grid">
    <a class="btn btn-danger">
      {{ include('contrat/_delete_form.html.twig') }}
    </a>
  </div>
  
  
    </div>
    <div class="col-sm-6">
      <div class="d-grid">
        <a href="{{ path('app_contrat_index') }}" class="btn btn-success">
          <i class="bi bi-arrow-left"></i> Back to List
        </a>
      </div>
    </div>
  </div>
    

   
            <!-- End small tables -->

            </div>
        </div>

    </div>



</div>
{% endblock %}


{% extends 'base1.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
<div style="margin-left:600px ; width:40% ; margin-top:300px ">
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Type</th>
                <td>{{ abonnement.type }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ abonnement.duree }}</td>
            </tr>
            <tr>
                <th>Frais</th>
                <td>{{ abonnement.frais }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_abonnement_index') }}">back to list</a>

    <a href="{{ path('app_abonnement_edit', {'id': abonnement.id}) }}">edit</a>
</div>
    {{ include('abonnement/_delete_form.html.twig') }}
    
{% endblock %}
